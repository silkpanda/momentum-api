# --- Mandatory Database Configuration (Phase 1.2) ---
# CRITICAL: Connection string for your MongoDB Atlas (M0 tier is mandated by the blueprint)
# Replace <username>, <password>, and <cluster-url> with your Atlas credentials.
# The server.ts file will crash if this variable is missing.
MONGO_URI=mongodb+srv://anthony_admin:PVGZ0HGU9XTjru6h@momentum.hhkukjg.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=Momentum

# --- Mandatory Server Configuration ---
# The port the Node.js/Express API will listen on.
PORT=3000

# --- Mandatory Authentication Configuration ---
# CRITICAL: A long, random, and unguessable string for signing JWTs.
# The default fallback is 'YOUR_SUPER_SECURE_DEFAULT_SECRET' but should be replaced for security.
JWT_SECRET=your_long_random_jwt_secret_here

# The expiration time for the JWT (e.g., 3 days).
# This uses the 'ms' library format (3d = 3 days, 1h = 1 hour).
JWT_EXPIRES_IN=3d